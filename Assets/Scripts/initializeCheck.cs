using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class initializeCheck : MonoBehaviour
{

    //this is an empty script, but we do actually use this to check if the AR scene has already loaded. find the logic in sceneLoader.
}
